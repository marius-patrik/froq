[package]
license = "Apache-2.0"
name = "froq-version"
version = "0.2.110"
edition.workspace = true
description = "Lockstepped grok CLI version."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
